name: authentication
description: "A new Flutter package project."
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.10.1
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^5.0.0
  shopee_core: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0